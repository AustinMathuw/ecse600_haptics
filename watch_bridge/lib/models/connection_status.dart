enum SessionState {
  idle,
  active,
  stopping,
}

enum WebSocketConnectionStatus {
  disconnected,
  connecting,
  connected,
  error,
}

enum WatchConnectionStatus {
  unpaired,
  paired,
  connecting,
  error,
}
